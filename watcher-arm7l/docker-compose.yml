version: '3.6'
services:
  watcher:
    image: 'wallseven/watcher:latest'
    container_name: watcher_container_arm7l
    env_file:
      - ../envfile
    volumes:
      - './test-dynamique/:/opt/robots/:rw'
